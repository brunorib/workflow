language: python
python:
  - "3.7"
services:
  - postgresql
# command to install dependencies
install:
  - python setup.py install
before_script:
  - createdb test_db
# command to run tests
script:
  - pytest